# Dev-only override: publishes Postgres on localhost for `just db-up`.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d postgres
services:
  postgres:
    ports:
      - "5432:5432"
